from setuptools import setup, find_packages

setup(
  name='Tris',
  version='0.0.1',
  packages=find_packages(),
  install_requires=[
    # Add other dependencies here
    # e.g. 'numpy', 'pandas'
  ]
)